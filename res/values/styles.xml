<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="Theme.AppCompat.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>

    <style name="custom_style_parent" parent="@android:style/TextAppearance">
        <item name="android:textSize">30sp</item>
        <item name="attr_x">@drawable/e</item>
        <item name="attr_str">AAAAA</item>
    </style>

    <style name="custom_style" parent="custom_style_parent">
        <item name="android:gravity">center</item>
        <item name="android:textColor">@color/abc_search_url_text_holo</item>
        <item name="attr_str">BBBBBB</item>
        <item name="attr_x">@drawable/custom_bitmap</item>
    </style>

</resources>